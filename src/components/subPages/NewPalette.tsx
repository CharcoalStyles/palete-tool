import { Color } from "chroma-js";

type NewPaletteProps = {
  onNewPalette: (NewPalette: Array<Color>) => void;
};

export const NewPalette = () => {
  return <div className="flex flex-col gap-4">
    <div>
      
    </div>
  </div>;
};
